# =================================================================================
# 🏗️ RBAC管理员服务器 - 环境变量配置示例
# =================================================================================
# 📋 使用说明：
#   1. 将此文件复制为 .env 文件
#   2. 修改为你自己的配置值
#   3. 生产环境请使用实际的安全值
# =================================================================================

# 🖥️ 系统配置
SYSTEM_PORT=8080

# 🗄️ 数据库配置
DB_MODE=mysql                    # 数据库类型: mysql, postgres, sqlite
DB_HOST=localhost               # 数据库主机地址
DB_PORT=3306                    # 数据库端口
DB_USERNAME=root                # 数据库用户名
DB_PASSWORD=your_database_password_here  # 数据库密码（必填）
DB_NAME=rbacadmin                # 数据库名称
DB_PATH=./data/rbac_admin.db     # SQLite数据库路径（当DB_MODE=sqlite时使用）

# 🔐 JWT配置 - 使用强密钥
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters  # JWT密钥（必填，建议32位以上）
JWT_EXPIRE_HOURS=24
JWT_REFRESH_EXPIRE_HOURS=168
JWT_ISSUER=rbac-admin
JWT_AUDIENCE=rbac-admin

# 🔄 Redis配置
REDIS_ADDR=localhost:6379       # Redis地址
REDIS_PASSWORD=                 # Redis密码（如无密码留空）
REDIS_DB=0                      # Redis数据库编号

# 📝 日志配置
LOG_LEVEL=info
LOG_DIR=./logs

# 🎯 应用配置
APP_NAME=RBAC管理员
APP_VERSION=1.0.0
APP_ENVIRONMENT=production
APP_DEBUG=false

# 🔒 安全配置
CSRF_SECRET=your_csrf_secret_key_here  # CSRF密钥（必填）

# 🌐 CORS配置
CORS_ORIGINS=https://your-domain.com